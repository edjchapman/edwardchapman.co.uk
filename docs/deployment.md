# Deployment

<!-- Stub — expanded in Phase 0 PR-4 and Phase 1 (domain cutover runbook). -->

Deploys run from GitHub Actions with wrangler. Production deploys from `main`
only, behind the `production` environment and green required checks.
